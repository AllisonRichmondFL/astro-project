---
interface Props {
	emoji?: string;
	label: string;
	url: string;
}
const { emoji, label, url } = Astro.props;
---

<a href={url} class="inline-block py-1 px-1 rounded hover:bg-neutral-100 hover:no-underline text-inherit"
	><span aria-label={label}>{emoji}</span></a
>
